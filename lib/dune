(ocamllex lexer)

(menhir (modules parser)
  )

(library
 (name lib)
 (preprocess (pps ppx_deriving.show ppx_sexp_conv ppx_compare))
 (libraries printbox printbox-text))

(env
  (dev
    (flags (:standard -warn-error -A)))) 
